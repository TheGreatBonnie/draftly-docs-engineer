# Secrets - never commit
draftly_accessKeys.csv

# Git submodules (registered via .gitmodules) — do not ignore
# authly/, draftly-agent-backend/, draftly-agent-ui/

# authly-scenarios/ are local git worktrees of the authly submodule (one per
# feat/* branch). They are never pooled into the superproject; create them with:
#   git -C authly worktree add ../authly-scenarios/001-oauth-login feat/001-oauth-login
/authly-scenarios/

# Other nested repos not yet wired as submodules
/draftly-agent-frontend/
/onboarding-flow-designs/

# Regenerable / generated
/graphify-out/
.pytest_cache/
.ruff_cache/
__pycache__/
*.pyc

# OS / editor
.DS_Store

# Transient session / accidental files
session-ses_f9a9.md
--full-page